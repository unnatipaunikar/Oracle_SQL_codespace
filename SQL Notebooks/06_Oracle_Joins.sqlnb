cells:
  - kind: 2
    value: |
      SELECT
      a. id id_a,
      a. color color_a,
      b. id id_b,
      b. color color_b
      FROM
      palette_a a
      NNER JOIN palette_b b ON a.color = b. color;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      a-id id_a,
      a. color color_a,
      b. id id_b,
      b. color color_b
      FROM
      palette_a a
      LEFT JOIN palette_b b ON a. color = b. color;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      a. id id_a,
      a. color color_a,
      b. id id_b,
      b. color color_b
      FROM
      palette_a a
      LEFT JOIN palette_b b ON a.color = b.color
      WHERE b. id IS NULL;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      a. id id_a,
      a. color color_a,
      b. id id_b,
      b. color color_b
      FROM
      palette_a a
      RIGHT JOIN palette_b b ON a.color = b. color;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      a. id id_a,
      a. color color_a,
      b. id id_b,
      b. color color_b
      FROM
      palette_a a
      RIGHT JOIN palette_b b ON a. color = b.color
      WHERE a.id IS NULL;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      a. id id_a,
      a. color color_a,
      b.id id_b,
      b. color color b
      r-R0M
      palette_a a
      FULL OUTER JOIN palette_b b ON a. color
      = b. color;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      a,id id_a,
      a. color color_a,
      b. id id_b,
      b. color color_b
      FROM
      palette_a a
      FULL JOIN palette_b b ON a.color = b.color
      WHERE a.id IS NULL OR b.id IS NULL;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      *
      FROM
      T1
      INNER JOIN T2 ON join_predicate;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      *
      FROM
         orders
      INNER JOIN order_items ON
         order_items. order_id = orders. order_id
      ORDER BY
         order_date DESC;
    languageId: oracle-sql
  - kind: 2
    value: order_items.order_id = orders.order_id
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      *
      FROM
      T1
      INNER JOIN T2 USING( cl, c2,...);
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      *
      FROM
      orders
      INNER JOIN order_items USING( order_id);
      ORDER BY
      order_date DESC;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
         name AS customer_name,
         order_id,
         order_date,
         item_id,
         quantity,
         unit_price
      FROM
         orders
      INNER JOIN order_items USING(order_id)
      INNER JOIN customers USING(customer_id)
      ORDER BY
            order_date DESC,
            order_id DESC,
            item_id ASC;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          name AS customer_name,
          order_id,
          order_date,
          item_id,
          product_name,
          quantity,
          unit_price
      FROM
          orders
      INNER JOIN order_items
                 USING(order_id)
      INNER JOIN customers
                 USING(customer_id)
      INNER JOIN products
                 USING(product_id)
      ORDER BY
      order_date DESC,
      order_id DESC,
      item_id ASC;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          coltnn_list
       FROM
          T1
      LEFT JOIN T2 ON
            join_predicate;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          order_id,
          status,
          first_name,
          last_name
      FROM
         orders
      LEFT JOIN = employees ON employee_id salesman_id
      ORDER BY
      order_date DESC;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          order_id,
          name AS customer_name,
          status ,
          first_name,
          last_name
      FROM
          orders
      LEFT JOIN employees ON
           employee_id = salesnan_id
      LEFT JOIN customers ON
           customer.customer_id = orders.customer_id
      ORDER BY
            order_date DESC;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
           column_list
      FROM
         T1
      LEFT JOIN T2 USING(c1,c2,c3, ...);
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
           column_list
      FROM
          t1
      LEFT JOIN t2 ON
          t1.c1 = t2.c1
          AND tl.c2 = t2.c2
          AND t1.c3 =t2.c3
          AND ...;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          name,
          order_id,
          status,
          order_date
      FROM
          customers
      LEFT JOIN orders
                USING(customer_id)
      ORDER BY
            name;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
           order_id,
           status,
           employee_id,
           last_name
      FROM
          orders
      LEFT JOIN employees ON
           enployee_id = salesman_id
      WHERE
          order_id = 58;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
           order_id,
           status'
           employee_id,
           last_name
      FROM
          orders
      LEFT JOIN employees ON
           enployee_id = salesman_id
      AND order_id = 58;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
         coltnn_list
      FROM
         T1
      RIGHT OUTER JOIN T2 ON
            join_predicate;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          first_name,
          last_name,
          order_id,
          status
      FROM
         orders
      RIGHT JOIN employees ON
            employee_id = salesman_id
      WHERE
          job_title ='Sales Representative'
      ORDER BY
            first_name,
            last_name;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          column_list
      FROM
         T1
      RIGHT OUTER JOIN T2 USING(c1,c2,c3);
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      column_list
      FROM
      T1
      RIGHT OUTER JOIN T2 ON
      Tl.cl - T2.cl
      AND Tl.c2 - T2.c2
      AND Tl.c3 - T2.c3;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
         name,
         order_id,
         status
      FROM
           orders
      RIGHT JOIN customers
           USING(customer_id)
      ORDER BY
          name;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          employee_id,
          last_name,
          first_name,
          order_id,
          status
      FROM
         orders
      RIGHT JOIN employees ON
             employee_id = salesman_id
      WHERE
          employee_id = 57;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
          column_list
      FROM
         T1
      CROSS JOIN T2;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
           product_id,
           warehouse_id,
           ROUND(dbms_random.value(10,100)) quantity
      FROM
          products
      CROSS JOIN warehouses;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
         column_1ist
      FROM
         T t1
      INNER JOIN T t2 ON
      j  oin_predicate ;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      (e. first_name || ' ' || e.last_name) employee,
      (m. first_name || ' ' || m.last_name) manager,
      e. job_title
      FROM
      employee e
      LEFT JOIN employee m ON
      m.employee_id = e.manager_id
      ORDER BY
      manager;
    languageId: oracle-sql
  - kind: 2
    value: |
      SELECT
      el. hire_date,
      (el.first_næme || ' ' || el.last_name) employee1,
      (e2.first_name || ' ' || e2.last_name) employee2
      FROM
      employees el
      INNER JOIN employees e2 ON
      e1.employee_id> e2.employee_id
      AND el.hire_date = e2.hire_date
      ORDER BY
      e1.hire_date DESC'
      employee1,
      employee2;
    languageId: oracle-sql
